# Claude Code permissions: machine-local, not part of the project
.claude/settings.local.json

# OS noise
Thumbs.db
.DS_Store

# In case node tooling ever lands here
node_modules/
*.log


# Notes taken while reading that clone; its file paths only resolve locally.
docs/awakened-poe-trade-analisis.md

# Positioning notes to self, including what not to claim. Useful internally,
# odd in a public repo.
docs/resumen-producto.md

# Rate-limit state shared between tool runs (see tools/lib/shared-limits.mjs)
tools/.limits.json

# Built by tools/package.mjs, not source
dist/
